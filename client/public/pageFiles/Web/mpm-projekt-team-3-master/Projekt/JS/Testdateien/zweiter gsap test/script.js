// gsap.from('#lock1', {duration: 5, y: '-500%',ease: 'back' })

/*
$(".icon-lock").on("click", function(){
    $(gsap.to(".active", {duration: 5, y: '-500%',ease: 'back' }));
});
*/

/*
let targets = gsap.utils.toArray(".icon-lock");

targets.forEach((obj) => {
    obj.anim = gsap.to(".lock-top-1", { duration: 1, y: '500%',ease: 'back'  }).reversed(true);
    obj.addEventListener("click", doCoolStuff);
});

function doCoolStuff() {
    this.anim.reversed(!this.anim.reversed());
}
 */


