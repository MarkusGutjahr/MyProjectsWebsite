package de.thm.mov2game

import de.thm.mov2game.model.entities.Entity
import de.thm.mov2game.model.items.Item
import de.thm.mov2game.model.items.ItemType
import de.thm.mov2game.model.items.StatModifier
import org.junit.Test
import org.junit.Assert.*

class EntityTest {
/*
    @Test
    fun attack() {
        val entity1 = Entity("Entity1", 100, 20, 0, 0.0)
        val entity2 = Entity("Entity2", 100, 15, 0, 0.0)

        entity1.attack(entity1, entity2)

        assertEquals(80, entity2.health)
    }
    @Test
    fun testTakeDamage() {
        val entity = Entity("TestEntity", 100, 10)
        entity.takeDamage(20)
        assertEquals(80, entity.health)
    }


    @Test
    fun testMove() {
        val entity = Entity("TestEntity", 100, 10)
        entity.move(2, 3)
        assertEquals(2, entity.currentPositionX)
        assertEquals(3, entity.currentPositionY)
    }


    @Test
    fun testAddItemToInventory() {
        val entity = Entity("TestEntity", 100, 10)
        val healthPotion = Item("Health Potion", "Restores 20 health points", ItemType.POTION, StatModifier(healthModifier = 20))
        entity.addItem(healthPotion)
        assertEquals(1, entity.inventory.size)
        assertEquals(healthPotion, entity.inventory[0])
    }

    @Test
    fun testGainExperience() {
        val entity = Entity("TestEntity", 100, 10)
        entity.gainExperience(50)
        assertEquals(50, entity.experience)
        entity.gainExperience(70)
        assertEquals(120, entity.experience)
    }
 */
}
